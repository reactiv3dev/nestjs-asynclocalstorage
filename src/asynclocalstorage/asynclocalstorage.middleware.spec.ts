import { AsynclocalstorageMiddleware } from './asynclocalstorage.middleware';

describe('AsynclocalstorageMiddleware', () => {
  it('should be defined', () => {
    expect(new AsynclocalstorageMiddleware()).toBeDefined();
  });
});
